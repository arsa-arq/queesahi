{
  "name": "que-es-ahi",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Exploración urbana geolocalizada del centro histórico de Bogotá.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "dev": "node scripts/serve.mjs",
    "validate": "node scripts/validate-places.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "typecheck": "tsc -p jsconfig.json",
    "export:json": "node scripts/export-json.mjs",
    "check": "npm run validate && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
